import React from "react";
import { ReactComponent as Clear } from "../../assets/clear.svg";

const ClearIcon: React.FC = () => {
  return <Clear className="ClearIcon" width={28} height={28} />;
};

export default ClearIcon;
